package Test.Collectors;

import java.util.Arrays;
import java.util.List;

public class Collectors {

    public static void main(String[] args) {
//
        List<String> givenList = Arrays.asList("a", "bb", "ccc", "dd", "bb", "dd");
//
//        List<String> resultList = givenList.stream()
//                .collect(toList());
//
//        //
//        Set<String> resultSet = givenList.stream()
//                .collect(toSet());
//
//
//        System.out.println(resultList);
//        System.out.println(resultSet);

//
//        Map<String, Integer> result = givenList.stream()
//                .collect(toMap(Function.identity(), String::length, (item, identicalItem) -> item));

//        List<String> result = givenList.stream()
//                .collect(collectingAndThen(toList(), ArrayList::copyOf));

//
//
//        System.out.println(result);
//
//
    }



}
